const initialState = [
  {
    text: "First Todo",
    completed: false,
    id: 0
  }
];

const todos = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default todos;
